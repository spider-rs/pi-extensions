# direnv config for the pi-extensions workspace.
# Loads variables from the adjacent .env (e.g. SPIDER_API_KEY) into the shell
# whenever you cd into this directory, and unloads them when you leave.
# After editing, run: direnv allow
dotenv
